import {Bounce} from "react-toastify";

export  const styleToastr = {
    position: "top-right",
    autoClose: 3000,
    theme: "dark",
    transition: Bounce,

}

export  const styleToastrSuccess = {
    position: "bottom-right",
    autoClose:3000,
    theme: "dark",
    transition: Bounce,
}